services:
  api:
    build: ./Backend       
    ports:
      - "8000:8000"
    volumes:
      - ./Backend:/Backend
    command: bash -c "python manage.py runserver 0.0.0.0:8000"

  web:
    build: ./Frontend
    ports:
      - "3000:3000"
    volumes:
      - ./Frontend:/Frontend